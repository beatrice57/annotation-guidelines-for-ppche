<!doctype html><!--DONE-->
<html>
<head>
<link rel="stylesheet" href="manual.css">
<title>Known issues</title>
</head>

<body>

<a name="top">
<h2>Known issues</h2>

<ul>

<a name="examples">
<p>
<li>
Due to changes in sentence tokenization, the running ID numbers in the
examples in the documentation no longer always match the current ID
numbers.  If necessary, the examples can be traced back to their source
via the page numbers in the IDs, which are stable.

<p>
<li> ACCORDING (TO), CONCERNING, REGARDING, and TOUCHING in their
quasi-prepositional uses are not annotated alike.  ACCORDING (TO) and
REGARDING are tagged <tt>VAG</tt> and lemmatized as verbs, whereas
CONCERNING and TOUCHING are tagged <tt>P</tt> and lemmatized as
prepositions.

<p>
<li>
There likely remain inconsistencies in the annotation
of <a href="words.html#all">ALL</a> in its floated quantifier
vs. intensifier use, both within PPCME2 and between PPCME2 and the later
corpora.

<p>
<li>
The <tt>-PRN</tt> dash tag on <a href="syn-sub.html#cp-adv_as_x_said">AS X
SAID</a> and similar parenthetical expressions is likely appended
inconsistently, especially in the later corpora.

<p>
<li>
<a href="syn-conj.html#backwards_gapping">Backwards gapping</a> may be
mistagged as <a href="syn-conj.html#gapping">ordinary gapping</a>.

<p>
<li>
<a href="changes.html#adj-ppl">Departicipial adjectives</a> (INTERESTED,
INTERESTING) in predicate position may be mistagged as participles
(<tt>VAG</tt>, <tt>VAN</tt>), especially in the PPCME2.

<p>
<li>In <a href="syn-sub.html#cp-cmp_elision_brackets">comparative clauses with
elision</a>, brackets around elided subordinate clauses are likely to be
missing.

<p>
<li>
<a href="syn-const.html#spe">Direct speech</a> fragments may be
labeled <tt>FRAG</tt> rather than <tt>QTP</tt>.

<!--
<a name="elided-for">
<p>
<li>
<a href="syn-empty.html#elided_for_to">Elided FOR and TO in conjoined
infinitives</a> may be missing.
-->

<!--
<a name="ecm">
<p>
<li>
<a href="syn-sub.html#ecm_or_obj_control">Exceptional Case-Marking
(ECM) versus object control</a>
-->

<p>
<li>
<a href="syn-sub.html#ip-inf-adt">Infinitival adjuncts</a>
(<tt>IP-INF-ADT</tt>) may be missing the <tt>-ADT</tt> tag.

<p>
<li>
In searches for <a href="syn-const.html#np-msr">measure phrases</a>
(<tt>NP-MSR</tt>), it is wise to include <tt>QP</tt>, and vice versa.

<p>
<li>
Many inconsistencies and outright errors likely remain in connection
with <a href="pos.html#noun_proper">proper nouns</a> (<tt>NPR</tt>).

<p>
The guidelines for proper nouns of the
form <nobr><a href="pos.html#noun_by_form_the_n_of_np">THE N OF
NP</a></nobr> (THE WAR OF THE ROSES) have the counterintuitive result that
none of the nouns is tagged <tt>NPR</tt>.

<p>
<li>
<a href="syn-conj.html#pseudo-imp">Pseudo-imperatives</a> are not
annotated in the PCEEC.

<p>
<li>
<a href="syn-sub.html#ip-inf-prp">Purpose infinitives</a>
(<tt>IP-INF-PRP</tt>) may be missing the <tt>-PRP</tt> dash tag,
particularly in connection with GO and SEND.

<a name="ip-ppl">
<p>
<li>
In searches for <a href="syn-sub.html#ip-ppl_or_rrc">reduced relative
clauses</a> (<tt>RRC</tt>), it is wise to consider including ordinary
participial clauses (<tt>IP-PPL</tt>), and vice versa.

<p>
<li>
Instances of <a href="syn-conj.html#right_node_raising">right node
raising</a> may be missing the index required by the guidelines,
particularly in the statutes.

<p>
<a name="spr">
<li>
Secondary predicates
(<a href="syn-const.html#adjp-spr"><tt>ADJP-SPR</tt></a>,
<a href="syn-const.html#np-spr"><tt>NP-SPR</tt></a>) may be mistagged as
simple predicates of <a href="syn-sub.html#ip-smc">small clauses</a>
(<tt>IP-SMC</tt>), and vice versa.
  
</ul>

</body>
</html>

